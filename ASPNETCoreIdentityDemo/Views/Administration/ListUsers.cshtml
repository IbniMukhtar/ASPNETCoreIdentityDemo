@model ASPNETCoreIdentityDemo.Models.ViewModels.UserListViewModel
@{ 
    ViewBag.Title = "All Users";
}
<style>
    .table td{
        padding:5px;
    }
</style>

@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success mt-5">
        <a href="#" class="close" data-dismiss="alert" aria-label="close">×</a>
        <p>@TempData["SuccessMessage"]</p>
    </div>
}

<div class="mb-3">
    <h3>All Users :<b class="text-success">@ViewBag.AllUsersCount</b></h3>
    @if (Model.ApplicationUser.AllUsers.Any())
    {
        <div class="row justify-content-between">
            <div class="col-md-6">
                <a asp-action="Register" asp-controller="Account" class="btn btn-primary mb-3">
                    Add New User
                </a>
            </div>
            <div class="col-md-4 text-md-right p-2">
                <div class="input-group">
                    <form method="post" id="searchForm" asp-action="Search" asp-controller="Administration" class="w-100">
                        <div class="input-group-append w-100">
                            <input type="search" name="searchTerm" placeholder="Search..." class="form-control" />
                            <button type="submit" class="btn btn-primary" data-mdb-ripple-init>
                                <i class="fa fa-search"></i>
                            </button>
                            <label class="form-label visually-hidden">Search</label>
                        </div>
                    </form>
                </div>
            </div>
        </div>

        <div class="table-responsive">
            <table class="table">
                <thead>
                    <tr>
                        <th>Action</th>
                        <th>First Name</th>
                        <th>Last Name</th>
                        <th>Email</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var user in Model.ApplicationUser.AllUsers)
                    {
                        <tr class="p-2 m-2">
                            <td>
                                <form method="post" asp-action="DeleteUser" asp-controller="Administration" asp-route-UserId="@user.Id">
                                    <a asp-action="EditUser" asp-controller="Administration"
                                       asp-route-UserId="@user.Id" class="btn btn-primary"><i class="fa fa-edit"></i></a>

                                    <span id="confirmDeleteSpan_@user.Id" style="display:none">
                                        <span>Are you sure you want to Delete?</span>
                                        <button type="submit" class="btn btn-danger">Yes</button>
                                        <a href="#" class="btn btn-primary" onclick="confirmDelete('@user.Id', false);return false;">No</a>
                                    </span>

                                    <span id="deleteSpan_@user.Id">
                                        <a href="#" class="btn btn-danger" onclick="confirmDelete('@user.Id', true);return false;">
                                            <i class="fa fa-trash"></i></a>
                                    </span>
                                </form>
                            </td>
                            <td>@user.FirstName</td>
                            <td>@user.LastName</td>
                            <td>@user.Email</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
    else
    {
        <div class="alert alert-info" role="alert">
            No Users Added Yet
        </div>
    }
   @Html.Partial("_Pagination", Model.Pagination)
</div>



@* this is for dynamic rows selection in a page   
<script>
    $(document).ready(function () {
        $('#updatePageSizeBtn').click(function () {
            var pageSize = $('#pageSizeInput').val();
            window.location.href = '@Url.Action("ListUsers", "Administration")?page=1&pageSize=' + pageSize;
        });
    });
</script> *@
<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<script>
    function confirmDelete(uniqueId, isTrue) {
        var deleteSpan = 'deleteSpan_' + uniqueId;
        var confirmDeleteSpan = 'confirmDeleteSpan_' + uniqueId;

        if (isTrue) {
            $('#' + deleteSpan).hide();
            $('#' + confirmDeleteSpan).show();
        } else {
            $('#' + deleteSpan).show();
            $('#' + confirmDeleteSpan).hide();

            // Trigger the form submission when "Yes" is clicked
            $('form[asp-route-UserId="' + uniqueId + '"]').submit();
        }
        return false;
    }

    $(document).ready(function () {
        var alertElement = $(".alert");

        // Check if the alert has already been manually closed
        if (!alertElement.hasClass("manual-close")) {
            // Automatically close the alert after a delay
            alertElement.delay(5000).slideUp(200, function () {
                alertElement.alert('close');
            });
        }

        // Handle manual closing through the close button
        alertElement.on("close.bs.alert", function () {
            console.log("Alert is being closed manually");
            // Add any additional logic you may need before the alert is closed
            alertElement.addClass("manual-close"); // Mark as manually closed
        });

        alertElement.on("closed.bs.alert", function () {
            console.log("Alert is closed");
            // Add any additional logic you may need after the alert is closed
        });
    });
</script>
